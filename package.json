{
  "name": "sh2-graph",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "vis": "^4.21.0"
  },
  "devDependencies": {
    "jest": "^24.8.0",
    "parcel": "^1.12.3",
    "rimraf": "^2.6.3"
  },
  "scripts": {
    "build": "parcel build -t browser -d dist index.html && rimraf dist/*.png",
    "watch": "parcel watch -t browser -d dist index.html",
    "clean": "rimraf dist/**"
  }
}
